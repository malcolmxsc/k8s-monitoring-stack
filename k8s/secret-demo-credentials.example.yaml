apiVersion: v1
kind: Secret
metadata:
  name: los-app-demo-credentials
  namespace: observability-sandbox
type: Opaque
stringData:
  # Demo/basic credentials for the application.
  # Replace these values locally and apply as k8s/secret-demo-credentials.yaml (do NOT commit real secrets)
  app-user: demo
  app-password: change-me

